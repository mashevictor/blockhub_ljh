{
  "name": "blockhub-e2e",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:api": "playwright test publish-runtime-plaza custom-capability plaza-interactions",
    "test:browser": "playwright test admin-login runtime-web-render runtime-mobile-h5 home-shell home-publish",
    "prepare:browsers": "playwright install chromium",
    "test:all": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
