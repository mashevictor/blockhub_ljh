# 积木仓 BlockHub · 6 周全量攻坚计划（合成版 v1.3）

> 合成来源：`16-TrackChat-6周全量攻坚计划.html`（母版 v5.1，D1–D36 日计划骨架）+ `项目计划-v1.2.html`（截至 2026-07-08 进度修订）。
> 当前快照：2026-07-13 · GA v0.2.0-ga · 2 全栈 · 主仓 `cozecode/` · staging `101.32.209.251` 已 signoff。
> 日历基线：D1 = 2026-07-02，GA 验收窗口 2026-08-07 ~ 08-12 **提前完成**。

状态图例：✅ 完成 · 🔶 进行/部分 · ⬜ 未开始 · 🔵 超前

---

## 0. 架构与 Web/Flutter 解耦设计（契约驱动）

**核心原则**：后端只产出两份契约——`page_schema`（UI 树）与 `capability_manifest`（包清单），二者都由同一份能力注册表派生。Web（runtime-web）与 Flutter（runtime-app）各自实现 Widget 注册表，按 `capability_key` 渲染，**互不依赖对方代码**。

| 层 | 内容 | 现状 |
|---|---|---|
| ① 单一能力源 | `backend/app/data/capability_registry.py` → `ALL_CAPABILITIES`（含 `widget/web_pkg/flutter_pkg/route/agent_id`） | 已落地 |
| ② 契约生成 | `schema_generator.py`（keys→page_schema）、`build_manifest.py`（keys→包清单：web=`@blockhub/web-capability-{slug}`，flutter=`capability_{key}`）、`GET /runtime/{id}/schema|manifest` | 已落地 |
| ③ Web 端 | `packages/web-core` WidgetHost + **13** 个解耦包 | 已落地 |
| ④ Flutter 端 | `runtime-app` WidgetRegistry + CAPABILITY_KEYS 菜单裁剪 + per-app APK | 已落地（物理 Melos 拆包 GA 后） |

**解耦红线**：① 后端永不直接写死某端 UI，必须经 schema+manifest；② Web/Flutter 不共享实现文件，只共享契约（schema 同构定义 + manifest JSON）；③ 新增能力时两端可独立排期、独立验收；④ W3 收口项（manifest 动态加载 + codegen TS/Dart）把契约固化为两端类型，防止长期漂移。

---

## 1. 进度快照（截至 2026-07-13 · GA）

- **W1 基建 100%**：✅ PG+Alembic 001–012 · JWT+RBAC · 114 Catalog+43 Capability PG · Chat SSE · 发布落库 · Redis 限流 · smoke 全绿。
- **W2 Agent ~95%**：✅ RAG+pgvector+citation、上海话语音 Agent、KB 上传+切片、审批 PG 状态机；🔶 custom 审核台 Admin UI 缺失。
- **W3 闭环 100%**：✅ schema_generator+build_manifest、runtime-web `/r/:appId`、13 Web 包、plaza 互动落 PG、Playwright E2E（含 home-publish）。
- **W4 七 Agent 100%**：✅ reports/notifications/integration 非 Mock、114 分页、tenant/config、stats 真实统计。
- **W5/W6 GA ~95%**：✅ Flutter auth/chat/approval/report + CAPABILITY_KEYS 裁剪 + flutter-build-custom · load-10vu P95 · ga-checklist 8+1 · server-ga-signoff；🔶 公网 HTTPS 证书 · 腾讯云 PG（用户暂跳过）。

---

## 2. 六周每日计划（D1–D36）

### W1 · 基建与鉴权（D1–D6，收尾 D7–D8）— ~88% ✅
| 天 | 全栈 A | 全栈 B | 状态 | 验收 |
|---|---|---|---|---|
| D1–D2 | Alembic + seed 114+43+7 · Catalog 读 PG | 登录页+JWT 拦截器 · Catalog 接 PG | ✅ | /catalog/summary 来自 DB |
| D3 | JWT+RBAC · users/tenants · 2 角色 seed | 路由守卫 · RoleGate 分流 | ✅ | 无 token 403 |
| D4 | LLM Gateway + Chat SSE 替换 Mock | Admin Chat 接真 SSE | ✅ | 流式可见 |
| D5 | Redis 限流 · COS 配置 | runtime-web 脚手架 Vite | 🔶 | Redis 可降级 |
| D6 | 应用/会话/消息表 · publish 写 PG · 去内存 | Flutter ConfigService 骨架 | ✅ | 重启数据仍在 |
| D7–D8 | AuthContext /me · dashboard COUNT · 2 workers · smoke 脚本 | 英雄弹幕加宽 · 登录联调 | ✅ | 28/28 · 首屏 24ms |

### W2 · Agent 核心：RAG + 审批（D7–D12）— ~55% 🔶
| 天 | 全栈 A | 全栈 B | 状态 | 验收 |
|---|---|---|---|---|
| D7–D9 | KB→COS · PyMuPDF 切片 · pgvector · /kb/search + RAG | 上传 UI · citation 气泡 · RAG 开关 | 🔵 超前 | topK+SSE 引用 |
| D9 | **新增** custom_capabilities 审核 API（custom_capability_store 已落地） | 审核台 UI | ⬜ | 后端就绪 · 前端 UI 缺失 |
| D10–D11 | approvals 状态机 · 去内存（含 demo seed） | 创建页接 feasibility · 审批列表 | 🔶 | 可提交 |
| D12 | 异步切片 · manifest codegen（JS/TS 已生成） | schema-renderer types + Registry | ✅ | 大 PDF 不阻塞 |
| 超前 | **新增** 上海话语音 Agent（TeleAI WS） | Home+runtime VoiceWidget | 🔵 | 语音 Demo |

### W3 · 发布闭环 + runtime-web（D13–D18）— ~70% 🔶
> M3：创建→webUrl→问答→请假审批主路径已通；两端共契约，零硬编码。
| 天 | 全栈 A | 全栈 B | 状态 | 验收 |
|---|---|---|---|---|
| D13 | schema_generator + capability_resolver · 12 模板 | web-core WidgetHost + ChatWidget | ✅ | JSON 合法 |
| D14 | publish→page_schema+build_manifest+webUrl | Form/Approval/List Widget | ✅ | DB 有 webUrl |
| D15 | GET /runtime/{id}/schema|manifest | runtime-web /r/:appId | ✅ | 浏览器打开员工端 |
| D16 | 20 场景模板 seed · apps.audience_type · plaza API | @受众 Picker · /plaza 壳 | 🔶 | feed 有数据 |
| D17 | feasibility 覆盖度评分 · @公开 写 feed | 三入口统一发布 · 双账号联调 | 🔶 | 行业/模块待对齐 |
| D18 | smoke W3 · plaza 联调 | PublishModal 预览 · 真 webUrl+深链 | 🔶 | API 冒烟通过 |
| **D8–D11 收口** | manifest 动态加载 · codegen TS/Dart · plaza 点赞评论落 PG | 三入口对齐 · Playwright E2E · 文档同步 | ✅ | **W3 已关闭** |

### W4 · 七 Agent 齐 + Catalog 全量（D19–D24）— 未开始 · 顺延 2 天
> M4：报表/通知/集成非 Mock · 114 分页 · 租户 Config · 广场互动落 PG。
| 天 | 全栈 A | 全栈 B | 验收 |
|---|---|---|---|
| D19 | reports 聚合 API（预置 SQL，COUNT 已优化） | 报表 Chart + StatCardWidget | 看板有数 |
| D20 | notifications PG · 审批触发 notify · feed 表 | 通知页 · plaza 点赞评论 API | 广场互动落 PG |
| D21 | integration REST connector + etl_jobs | 集成页 Timeline | 手动 sync 成功 |
| D22 | 65 办公+49 行业 scenario 全 PG · 模板映射 | 114 场景页 PG 分页 | count=114 |
| D23 | GET/PUT /tenant/config · theme/menu | 租户 Config 表单 | 改主题生效 |
| D24 | stats/dashboard 真实统计 | 创建向导 7 步与 /create 统一 | Dashboard 数字真 |

### W5 · Flutter + 腾讯云（D25–D30）— ~60% · Flutter 提前启动
> M5：Flutter WidgetRegistry 按 capability_key 消费同一份 manifest，与 web-core 平行解耦。
| 天 | 全栈 A | 全栈 B | 状态 | 验收 |
|---|---|---|---|---|
| D25 | CORS 生产域名 · audit_logs | Flutter：Dio+SSE 接 schema\|manifest · WidgetRegistry 按 key 注册（auth/login/app.dart 已落地） | ✅ | App 显示首页 |
| D26 | TencentDB PG+Redis 迁移 | Flutter 问答/审批 WidgetRegistry（chat/approval/sse 已落地） | ✅ | 云 DB 连通 |
| D27 | COS 生产 bucket · URL 签名 | Flutter 看板 fl_chart（依赖+report_page 已落地） | 🔶 | KB 云可传 |
| D28 | CVM Docker · uvicorn+worker | build home/admin dist | 🔶 | docker-compose.yml 已存在 |
| D29 | Nginx 三域名 SSL · SSE（自签 HTTPS 443 已提交） | Flutter 内测 APK · COS 分发 | 🔶 | HTTPS 200（自签）· APK 待打包 |
| D30 | staging 冒烟 · 环境变量文档 | Flutter+Web 同 appId 并行 | ✅ | 三端一致 |

### W6 · GA 回归与交付（D31–D36）— ✅ 提前完成
> M6 GA：114 回归 · 10 并发压测 · Compose · Demo · GA#9 裁剪 E2E。
| 天 | 全栈 A | 全栈 B | 状态 | 验收 |
|---|---|---|---|---|
| D31 | 114 回归脚本 | 12 模板行业 12 场景 UI | ✅ | regression-114 |
| D32 | 10 并发压测 · 限流 | runtime-web H5 适配 | ✅ | load-10vu P95 |
| D33 | docker-compose.prod + 备份 | Demo rehearsal | ✅ | pg-backup 脚本 |
| D34 | HTTPS 强制 · 脱敏 | PRODUCTION-DEPLOYMENT 实测 | 🔶 | IP 可达 · 证书待配 |
| D35–36 | GA 签字 · v0.2.0-ga tag | ga9-manifest-crop E2E | ✅ | signoff 通过 |

---

## 3. 广场 Newsfeed 专项（方案 B 桌面三栏）
- W3 D16–17：@受众选择 + /plaza 壳 + apps.audience_type 迁移 + @公开写 feed。
- W3 收口：plaza 点赞/评论落 PG（feed_posts/reactions/comments）+ Playwright E2E。
- W4 D20–21：PlazaPage 接 API + GET/POST /plaza/* + tenant 范围过滤。
- W6：热门 @ 标签 + @我 筛选 + 部门可见策略。

## 4. 双人分工
- **A**：PG · LLM/RAG · Agent Pipeline · 腾讯云后端 · 审批/报表/集成 API（12h 开发 + 4h 联调 + 2h 文档）。
- **B**：runtime-web · schema-renderer · Home/Admin · Flutter 联调 · 广场 UI（12h 前端 + 4h 联调 + 2h UI/Demo）。
- **共同**：W3/W6 端到端 · Schema 契约 Review · 每日 15min sync · 周日半天复盘。

## 5. GA 总验收（9 项）当前状态 · v0.2.0-ga
1. 114 Catalog — ✅ 114 全 PG 分页
2. 7 Agent 非 Mock — ✅ 七 Agent 真集成
3. 发布闭环 — ✅ Web+App E2E + home-publish UI
4. RAG — 🔵 基本可用
5. 审批 — ✅ 审批 PG · 通知联动
6. Flutter APK — ✅ 自选能力打包 + per-app build-queue
7. 腾讯云 HTTPS — 🔶 IP 公网可达 · 正式证书待配 · 云 PG 暂跳过
8. 文档 — ✅ deploy + smoke + RELEASE-v0.2.0-ga.md
9. 模块化裁剪 GA#9 — ✅ ga9-manifest-crop.spec.ts

## 6. 部署现状（101.32.209.251）
✅ Home / · ✅ Admin /admin/ · ✅ Runtime /r/:id · ✅ API :8001 · ✅ GA signoff · 🔶 域名 HTTPS · ⬜ 腾讯云 PG（暂跳过）

## 7. GA 后路线图（P1–P4）· 2026-07-13 更新
- **P1 生产正式化**：**已移出功能排期**（运维手册 `PRODUCTION-DEPLOYMENT.md`）。域名 HTTPS 已通；备份/cron 不占产品周次；JWT 轮换不做。
- **P2 产品**：✅ CI ga9/apk-status E2E · `smoke-build-manifest` · PR staging workflow · 🔶 go_router 依赖已加
- **P3 增长**：多租户 · Catalog CRUD · 插件 SDK · 国际化
- **P4 企业集成**：详见 §8 — Adapter 框架 + CRM Webhook + 企微/钉钉 + SSO · 约 4 周 · 可与 P1 并行

---

## 8. 企业系统集成落地计划（P4 · GA 后）

> **结论**：实现难度中等、工程量可控。骨架（Connector 模型 + Admin 管理面 + ingress/egress 数据流 + Capability 产品位）已就绪；剩余工作为按厂商写 Adapter，首个完成后后续基本是复制改 vendor。标准 REST **5–10 工作日**；复杂 ERP 中间表 **2–4 周**（需客户 DBA 配合）。

### 8.1 现状（P4-I1/I2 落地后）

| 层级 | 状态 | 说明 |
|------|------|------|
| 数据模型 | ✅ | `integration_connectors` + `etl_jobs` + `integration_events`（020） |
| API | ✅ | CRUD + Adapter `sync` + `ingress/webhook`（HMAC）+ `test-message` + events |
| Admin | ✅ | `IntegrationPage` 可编 `vendor` / `field_map` / webhook 密钥；job 展示真实 result |
| CRM 入站 | ✅ I1 | `POST /integrations/ingress/webhook` 验签落 `integration_events` |
| sync 执行 | ✅ I1 | `BaseConnectorAdapter` + registry；`CrmWebhookAdapter` / `ImWebhookAdapter` |
| IM 出站 | ✅ I2 | `im_delivery_service` 真 POST 钉钉/企微/飞书；审批/报修挂钩 |
| SSO | 🔶 I2 | `/auth/oauth/wecom/start|callback` → JWT；需 `WECOM_*` 环境变量 |
| 厂商 ERP | ⬜ I3 | 金蝶/用友按签约；目录预留 `connectors/adapters/` |

### 8.2 统一扩展架构

```
backend/app/services/connectors/
├── base.py              # BaseConnectorAdapter：extract → map → load
├── registry.py          # connector_type + vendor → adapter
└── adapters/
    ├── crm_webhook.py   # ✅ 通用 CRM Webhook + HMAC
    ├── im_webhook.py    # ✅ 企微/钉钉/飞书群机器人（统一）
    ├── erp_kingdee.py / erp_yonyou.py   # ⬜ I3
    └── auth_ldap.py                     # ⬜ I4
```

企微 SSO 在 `wecom_oauth.py` + `/auth/oauth/wecom/*`；冒烟：`scripts/smoke-integration-real.sh`。

`config_json` 统一 schema：`vendor` · `auth` · `field_map` · `tables` · `sync_mode` · `watermark`

数据流：`外部系统 → ingress/webhook 或 Adapter polling → 业务模块/Agent → egress 回写 CRM/IM`

**集成原则**（对齐 `integration-checklist.html`）：不替换核心 ERP/CRM；标准 API + 可选 Webhook；主数据只读同步、线索/工单双向、SSO 统一身份、IM 消息回写。

### 8.3 四类系统对接清单

| 类别 | 目标系统 | 对接模式 | 首个工期 | 后续同类 |
|------|----------|----------|----------|----------|
| **ERP/财务** | 用友 U8/YonBIP、金蝶云星空 | REST OpenAPI；U8 常走中间库只读视图 + watermark 增量 | 5–8 天（REST）| +3–5 天/厂商 |
| **CRM** | 纷享销客、销售易、自建 CRM | Webhook 入站 + HMAC 验签 + `field_map`；出站 egress 回写跟进 | 2–3 天（自建）| 5–7 天/商业 CRM |
| **协同** | 钉钉、企业微信、飞书 | 消息卡片推送 · 通讯录 sync · 审批 callback → ingress | 3–5 天（消息）| +2 天/平台 |
| **身份** | Azure AD、企微扫码、LDAP | OAuth2/OIDC `/auth/oauth/{provider}`；LDAP 私有化 `ldap3` | 3–5 天（企微）| Azure +2 天 |

**生产级必补**（琐碎但必要）：租户凭证加密存储 · Webhook 幂等/重试 · 字段映射 Admin UI · `EtlJob.result_json` 可读错误 · 客户沙箱联调。

### 8.4 四周排期（单人后端有经验 · 可并行前端）

| 周 | 里程碑 | 交付 | 验收 |
|----|--------|------|------|
| **I1** | Adapter 框架 | `BaseConnectorAdapter` + 替换 `run_sync()` 模拟逻辑；真实 `ingress/webhook` 入站 + 验签 | `smoke-integration-real.sh`：Webhook 入站落库 · sync job 有真实 result |
| **I1** | 通用 CRM | Generic Webhook Adapter + `field_map` 配置 | 自建 CRM POST 事件 → BlockHub 线索/工单可见 |
| **I2** | 协同通知 | `im_delivery_service`；企微/钉钉消息推送（`notify_im` 后端） | 审批触发 → IM 卡片送达 |
| **I2** | SSO 企微 | `/auth/oauth/wecom/start` + `/callback` → 现有 JWT | 企微扫码登录全流程 |
| **I3** | ERP（按签约） | 金蝶云星空或 YonBIP REST Adapter；主数据 sync | Admin「测试连接 / 同步主数据 / 查看映射」真调 API |
| **I3** | 前端收口 | `ConnectorWidget` 注册；`IntegrationPage` 字段映射编辑 | Widget 非演示 · Admin 可配 mapping |
| **I4** | 深化（可选） | 纷享/销售易 Webhook · Azure AD SSO · 通讯录全量 sync | 按首单客户选型验收 |

### 8.5 与 P1–P3 优先级关系

- **P4-I1/I2** 可与产品主线并行；运维项不阻塞功能周次
- **P4-I3** 依赖签约客户选型（金蝶 vs 用友），按项目触发
- GA 阶段「不做项」中的钉钉/企微生产级对接 → 本计划 **I2 起** 交付（原「签约 +2 周」收敛为 **I1+I2 ≈ 2 周** 可演示真联调）

### 8.6 验收标准（P4 收口）

1. `run_sync()` 不再模拟 — 至少 1 个 Adapter 产生真实 `records_synced` 与错误明细
2. `ingress/webhook` 至少 1 条入站路径验签通过并写入业务表
3. 企微或钉钉消息推送 — 审批事件触发 IM 通知（非仅站内 PG）
4. 企微扫码 SSO — 登录后 JWT 与现有 RBAC 对齐
5. Admin `IntegrationPage` — 连接器配置含 `vendor` + `field_map` 可编辑
6. 文档 — `integration-checklist.html` 与 OpenAPI/字段模板对齐
