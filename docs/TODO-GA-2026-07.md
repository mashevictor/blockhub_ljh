# BlockHub GA Todo List（2026-07-13）

---

## ✅ 已完成

| 模块 | 项 |
|------|-----|
| D12–D13 | custom 审核 + Playwright（含 home-publish 服务器通过） |
| D16 | regression-114 + P95 |
| D17 | smoke-ga / server-capability-test / plaza 去 mock |
| Web | **13** 物理包（含 creation / oa / audit / sso / mask） |
| Flutter | CAPABILITY_KEYS + blockhub.sh 自选打包 |
| 质量 | CI、ga-checklist、server-ga-signoff、smoke-web-packages |
| Release | v0.2.0-ga-rc1 + RELEASE note |

---

## 🔶 待你服务器确认（跑 signoff 即完成）

- [ ] `blockhub.sh signoff` 全套绿 → D17/D18 改 ✅
- [ ] GA 8/8 截图归档（人工）

---

## 📋 下一阶段 P1（生产正式化 · 脚本已就绪）

| 项 | 脚本 | 状态 |
|----|------|------|
| Web 包 100% | `blockhub.sh web-packages` | ✅ 代码完成 |
| PG 备份 | `blockhub.sh pg-backup` | ✅ 脚本完成 |
| JWT/密钥检查 | `blockhub.sh secrets-check` | ✅ 脚本完成 |
| 腾讯云 PG | `DATABASE_URL=... bash scripts/migrate-tencentdb.sh` | ⬜ 需云 DB 连接串 |
| 域名 HTTPS | Nginx + Let's Encrypt | ⬜ 运维 |
| 密码轮换 | 改 `.env` + `secrets-check` 通过 | ⬜ 运维 |

---

## 📋 下一阶段 P2（产品）

- [ ] Flavor 白标 / Shorebird（见 `docs/FLUTTER-MODULAR-APK.md`）
- [ ] Flutter deferred import 物理拆包
- [ ] CI browser E2E（需 staging `E2E_HOME_URL` secret）

---

## 服务器验收（复制）

```bash
cd /root/blockhub && git pull

# 全套 GA 签字
SKIP_APK=1 bash /root/blockhub/blockhub.sh signoff http://101.32.209.251

# 分项
bash /root/blockhub/blockhub.sh web-packages      # 13 Web 包
bash /root/blockhub/blockhub.sh secrets-check     # 生产密钥
bash /root/blockhub/blockhub.sh pg-backup         # PG 备份（需 pg_dump）
bash /root/blockhub/blockhub.sh flutter-build --list

# 腾讯云 PG（有连接串时）
export DATABASE_URL='postgresql+psycopg2://user:pass@xxx.tencentcdb.com:5432/trackchat?sslmode=require'
SMOKE_BASE=http://101.32.209.251 bash scripts/migrate-tencentdb.sh
```

---

## 变更记录

| 日期 | 内容 |
|------|------|
| 2026-07-13 | web-capability-creation、pg-backup、secrets-check、smoke-web-packages |
