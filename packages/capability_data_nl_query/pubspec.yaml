name: capability_data_nl_query
description: BlockHub capability module — data_nl_query (P1 Melos)
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  blockhub_flutter_core:
    path: ../blockhub_flutter_core
