name: capability_shanghai_voice
description: BlockHub capability module — shanghai_voice (P1 Melos)
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  blockhub_flutter_core:
    path: ../blockhub_flutter_core
  dio: ^5.7.0
  permission_handler: ^11.3.1
  record: ^6.0.0
  audioplayers: ^6.1.0
  web_socket_channel: ^3.0.2
