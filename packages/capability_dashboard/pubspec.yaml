name: capability_dashboard
description: BlockHub capability module — dashboard (P1 Melos)
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  blockhub_flutter_core:
    path: ../blockhub_flutter_core
  fl_chart: ^0.70.2
